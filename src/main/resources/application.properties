# JDBC Driver name.
spring.datasource.driver-class-name=org.postgresql.Driver
# Database connection string URL.
#spring.datasource.url=jdbc:postgresql://192.200.100.221:5432/university
spring.datasource.url=jdbc:postgresql://localhost:5432/university
# Database user's name.
spring.datasource.username=postgres
# Database user's password.
spring.datasource.password=postgres
# Number of ms to wait before throwing an exception if no connection is available.
spring.datasource.tomcat.max-wait=10000
# Maximum number of active connections that can be allocated from this pool at the same time.
spring.datasource.tomcat.max-active=50
# Validate the connection before borrowing it from the pool.
spring.datasource.tomcat.test-on-borrow=true

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.generate-ddl=true

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=maksymmykytuk@gmail.com
spring.mail.password=iwannasee
spring.mail.properties.mail.smtp.auth: true
spring.mail.properties.mail.smtp.starttls.enable: true
spring.mail.properties.mail.smtp.starttls.required: true
spring.mail.properties.mail.smtp.ssl.enable = true
spring.mail.test-connection=true
